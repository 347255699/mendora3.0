{
  "db": {
    "driverClass": "com.mysql.cj.jdbc.Driver",
    "user": "root",
    "password": "wokoworks002",
    "url": "jdbc:mysql://localhost:3306/pos_merchant?cachePrepStmts=true&useUnicode=true&characterEncoding=utf8"
  },
  "tables": [
    "platform_user",
    "platform_user_app",
    "platform_user_app_version"
  ],
  "targetPath": "/Users/pundix043/workbench/mendora/generator",
  "pojo": {
    "packageName": "org.mendora.generate",
    // noArgs, allArgs, reqArgs
    "constructor": "allArgs",
    // 是否需要 @toData, 默认不开启(注意: 该注解开启后会默认忽略其他Lombok注解)
    "dataAnnotation": false,
    // 是否开启链式, 默认开启
    "chainMode": true,
    "nonNull": [
      "id",
      "name"
    ],
    // 是否需要 @toString, 默认不开启
    "toStringAnnotation": false,
    // 是否需要 @Slf4j, 默认不开启
    "slf4jAnnotation": true
  }
}
